---

version: '3'

tasks:
  install:
    desc: "Install tools"
    cmds:
      - go build -v -o bin/newline-after-block github.com/breml/newline-after-block/cmd/newline-after-block
      - go build -v -o bin/lefthook github.com/evilmartians/lefthook/v2
      - go build -v -o bin/task github.com/go-task/task/v3/cmd/task
      - go build -v -o bin/golangci-lint github.com/golangci/golangci-lint/v2/cmd/golangci-lint
      - go build -v -o bin/gofumpt mvdan.cc/gofumpt
      - go build -v -o bin/commit-msg-lint github.com/breml/githooks/cmd/commit-msg-lint

  install-githooks:
    desc: "Install git hooks using lefthook"
    cmds:
      - bin/lefthook install

  format:
    desc: "Format code"
    aliases: [ fmt ]
    cmds:
      - bin/gofumpt -l -w .
      - bin/newline-after-block -fix ./...

  lint:
    desc: "Run linters"
    cmds:
      - markdownlint-cli2 --fix --files '**/*.md' '!.scratch/**' '!.beads/**' '!docs/**'
      - bin/golangci-lint config verify
      - bin/golangci-lint run --fix ./...

  test:
    desc: "Run tests"
    cmds:
      - go test -v -race -shuffle=on -cover -timeout=120s -parallel=10 ./...

  testacc:
    desc: "Run acceptance tests"
    summary: |
      Run the acceptance test suite, or one shard of it.

      Acceptance tests are dominated by Terraform CLI process overhead, not by
      Uptime Kuma, so they scale well beyond the number of available cores.

        task testacc                    # the whole suite
        task testacc PARALLEL=4         # lower the -parallel value (default 8)
        task testacc SHARD=1 SHARDS=4   # shard 1 of 4, as the CI matrix runs it

      Shards are derived from the actual test list, so every test lands in
      exactly one shard and a newly added test can never fall through a gap.
      This holds as long as test names are unique across packages; the
      discovery step below fails the run if they are not.
    env:
      TF_ACC: "1"
    vars:
      PARALLEL: '{{.PARALLEL | default "8"}}'
      SHARD: '{{.SHARD | default "1"}}'
      SHARDS: '{{.SHARDS | default "1"}}'
    cmds:
      - |
        set -euo pipefail
        run='.*'
        if [ '{{.SHARDS}}' -gt 1 ]; then
          # Discovery must not inherit TF_ACC: -list is handled inside
          # m.Run(), so TestMain would boot a whole Uptime Kuma container
          # just to print test names. Keep it in its own statement so a
          # failing build fails the shard instead of silently shrinking it.
          all=$(TF_ACC= go test -list '.*' ./...)
          all=$(printf '%s\n' "$all" | grep '^Test' | sort)
          dupes=$(printf '%s\n' "$all" | uniq -d)
          if [ -n "$dupes" ]; then
            echo "test names are not unique across packages, sharding would" \
              "run these more than once:" >&2
            printf '%s\n' "$dupes" >&2
            exit 1
          fi
          names=$(printf '%s\n' "$all" | \
            awk 'NR % {{.SHARDS}} == {{.SHARD}} % {{.SHARDS}}' | paste -sd '|')
          if [ -z "$names" ]; then
            echo "shard {{.SHARD}} of {{.SHARDS}} matches no tests" >&2
            exit 1
          fi
          run="^($names)\$"
        fi
        # The timeout stays below the 12-minute timeout-minutes on the "Run
        # acceptance tests" step in .github/workflows/test.yml, which also
        # covers the shard discovery above, so that a hung run is killed by Go,
        # with the goroutine dump that says where it hung, rather than by
        # GitHub, which only reports an exit code.
        go test -v -shuffle on -coverprofile coverage.out -timeout 600s \
          -parallel {{.PARALLEL}} -run "$run" ./...

  testacc-cover-report:
    desc: "Generate acceptance tests coverage report"
    preconditions:
      - test -f coverage.out
    cmds:
      - go tool cover -func=coverage.out | sort -k3 -rn

  clean:
    desc: "Clean build artifacts"
    cmds:
      - rm -f coverage.out

  build:
    desc: "Build all code"
    cmds:
      - go build -v ./...

  generate-docs:
    desc: "Generate documentation"
    dir: ./tools
    cmds:
      - go generate ./...
